{
  "name": "plantid",
  "version": "1.0.0",
  "private": true,
  "description": "A browser-based plant identification and health demo powered by Plant.id.",
  "scripts": {
    "dev": "node --env-file-if-exists=.env --watch server.js",
    "start": "node --env-file-if-exists=.env server.js",
    "check": "node --check server.js && node --check script.js"
  },
  "engines": {
    "node": ">=22"
  }
}
